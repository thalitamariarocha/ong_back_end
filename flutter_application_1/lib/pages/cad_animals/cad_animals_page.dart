import 'package:flutter/material.dart';

class CadAnimalsPage extends StatelessWidget {
  const CadAnimalsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Cadastro de Animais'),
      ),
    );
  }
}
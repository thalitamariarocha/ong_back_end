import 'package:flutter/material.dart';

class CadVoluntario extends StatelessWidget {
  const CadVoluntario({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Cadastro de Voluntários'),
      ),
    );
  }
}